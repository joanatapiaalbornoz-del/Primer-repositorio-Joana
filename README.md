# Primer-repositorio-Joana
Trabajo GitHub

**Estado del repositorio**

- **Remoto:** vinculado a `origin` (origin/main como upstream). ✅
- **Ramas relevantes:** `main`, `feature/navbar` (fusionada), `feature/footer` (PR abierto #1). 🔀
- **Pull request:** #1 — *feat(footer): agregar footer básico* (https://github.com/joanatapiaalbornoz-del/Primer-repositorio-Joana/pull/1). 🔗

**Cómo contribuir**

1. Crea una rama con prefijo `feature/` (ej. `feature/nombre`).
2. Haz commits con mensajes tipo `feat(...)` o `fix(...)`.
3. Abre un Pull Request desde tu rama hacia `main` y solicita revisión.

---

## Descripción del proyecto

**Primer-repositorio-Joana** — Pequeño proyecto web estático con un layout básico (navbar y footer) para practicar flujo de trabajo con Git/GitHub y colaboración.

### Resumen
- Proyecto estático en HTML/CSS para demostraciones y pruebas rápidas.
- Incluye: `index.html` con **navbar** y **footer** y un `README.md` con estado del repositorio.

### Características
- Navbar y footer básicos implementados.
- Estructura minimal: fácil de clonar, editar y desplegar.
- Buen ejemplo para aprender a crear ramas, abrir PRs y documentar cambios.

### Cómo usar
1. Clona el repositorio:
   - `git clone https://github.com/joanatapiaalbornoz-del/Primer-repositorio-Joana.git`
2. Abre `index.html` en tu navegador o sirve localmente:
   - `python -m http.server 8000` → abrir `http://localhost:8000`

> Nota: es un sitio estático; no requiere backend ni dependencias.

### Licencia y contacto
- Licencia: por definir (añade `LICENSE` si quieres especificarla).
- Contacto: abre un issue o PR en el repositorio.
**Estado del repositorio**

- **Remoto:** vinculado a `origin` (origin/main como upstream). ✅
- **Ramas relevantes:** `main`, `feature/navbar` (fusionada), `feature/footer` (PR abierto #1). 🔀
- **Pull request:** #1 — *feat(footer): agregar footer básico* (https://github.com/joanatapiaalbornoz-del/Primer-repositorio-Joana/pull/1). 🔗

**Cómo contribuir**

1. Crea una rama con prefijo `feature/` (ej. `feature/nombre`).
2. Haz commits con mensajes tipo `feat(...)` o `fix(...)`.
3. Abre un Pull Request desde tu rama hacia `main` y solicita revisión.

